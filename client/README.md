# React Client

Dev:

```bash
cd client
npm install
npm run dev
```

The API base URL is configured with Vite environment files.

Development uses:

```
VITE_API_BASE=http://localhost:3001/api
```

Production uses:

```
VITE_API_BASE=/api
```
