const express=require('express'),C=require('../controllers/HolidayController'),auth=require('../middleware/authMiddleware'),allow=require('../middleware/roleMiddleware');const router=express.Router();router.use(auth);const view=allow('admin','rrhh','contabilidad','gerencia'),manage=allow('admin','rrhh');router.get('/policy',view,C.getPolicy);router.put('/policy',manage,C.updatePolicy);router.get('/',view,C.getAll);router.post('/',manage,C.create);router.put('/:id',manage,C.update);module.exports=router;
