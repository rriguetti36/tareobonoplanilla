const{poolPromise,sql}=require('../config/db');const fail=(m,s=400)=>{const e=new Error(m);e.status=s;throw e};class BonusRuleService{static async update(id,c,d){if(!d.areaId||!d.bonusTypeId||!d.name||!d.validFrom)fail('Completa área, tipo, nombre y vigencia');const p=await poolPromise,locked=await p.request().input('id',sql.Int,id).input('c',sql.Int,c).query(`SELECT TOP 1 x.id FROM dbo.BonusCampaigns x WHERE x.ruleId=@id AND x.companyId=@c AND x.status IN(N'approved',N'closed')`);if(locked.recordset[0])fail('La regla ya fue usada por una campaña aprobada o cerrada',409);const r=await p.request().input('id',sql.Int,id).input('c',sql.Int,c).input('a',sql.Int,d.areaId).input('t',sql.Int,d.bonusTypeId).input('n',sql.NVarChar(120),d.name).input('m',sql.NVarChar(20),d.calculationMode).input('bs',sql.NVarChar(30),d.baseSource||'salary').input('f',sql.Decimal(12,2),d.fixedAmount||null).input('pc',sql.Decimal(7,4),d.percentage||null).input('mb',sql.Decimal(12,2),d.monthlyBonusBase||null).input('ad',sql.Int,d.minAttendanceDays||0).input('ab',sql.Int,d.maxAbsences||0).input('lt',sql.Int,d.maxLateArrivals||0).input('sn',sql.Int,d.minSeniorityDays||0).input('ew',sql.Int,d.entryWindowDays||30).input('pmin',sql.Decimal(5,2),d.productivityMin===''?null:d.productivityMin).input('pmax',sql.Decimal(5,2),d.productivityMax===''?null:d.productivityMax).input('vf',sql.Date,d.validFrom).input('vt',sql.Date,d.validTo||null).input('st',sql.NVarChar(20),d.ruleStatus||'draft').query(`UPDATE dbo.BonusRules SET areaId=@a,bonusTypeId=@t,name=@n,calculationMode=@m,baseSource=@bs,fixedAmount=@f,percentage=@pc,monthlyBonusBase=@mb,minAttendanceDays=@ad,maxAbsences=@ab,maxLateArrivals=@lt,minSeniorityDays=@sn,entryWindowDays=@ew,productivityMin=@pmin,productivityMax=@pmax,validFrom=@vf,validTo=@vt,ruleStatus=@st,updatedAt=SYSUTCDATETIME() OUTPUT INSERTED.* WHERE id=@id AND companyId=@c`);if(!r.recordset[0])fail('Regla no encontrada',404);return r.recordset[0]}}module.exports=BonusRuleService
